# Manual dependency installation

Because the environment could not reach npm during automation, please run the following locally before starting the API:

```
cd load-test-api
npm install pdfkit @types/pdfkit
```

These packages are required for PDF export support.
